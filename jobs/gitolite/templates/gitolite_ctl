#!/bin/bash

# Control script to start/stop ruby/rack gitolite job on a BOSH controlled VM
#
# In local development, this script can be run manually. Examples below assume you are in the root of this release project.
#
# Start process example:
# APP_DIR=/path/to/gitolite RACK_ENV=development COMMAND=shotgun PORT=9393 jobs/gitolite/templates/gitolite_ctl start
#
# View STDOUT/STDERR logs:
# jobs/gitolite/templates/gitolite_ctl logs
#
# Clear logs:
# jobs/gitolite/templates/gitolite_ctl clearlogs

# TODO - change "EXECUTABLE_SERVER" to the command to run

RUN_DIR=/var/vcap/sys/run/gitolite
LOG_DIR=/var/vcap/sys/log/gitolite
STORE=/var/vcap/store/gitolite
PIDFILE=$RUN_DIR/gitolite.pid

export PATH=/var/vcap/packages/ruby/bin:$PATH

COMMAND=${COMMAND:-/var/vcap/packages/gitolite/bin/EXECUTABLE_SERVER}
HOME=${HOME:-/home/vcap}

case $1 in

  start)
    mkdir -p $RUN_DIR
    mkdir -p $LOG_DIR
    mkdir -p $STORE

    echo $$ > $PIDFILE

    exec $COMMAND >>$LOG_DIR/gitolite.stdout.log 2>>$LOG_DIR/gitolite.stderr.log
    ;;

  stop)
    PID=$(head -1 $PIDFILE)
    kill $PID
    while [ -e /proc/$PID ]; do sleep 0.1; done
    rm -f $PIDFILE
    ;;

  logs)
    cat $LOG_DIR/*
    ;;

  tail)
    tail -f $LOG_DIR/*
    ;;

  clearlogs)
    rm $LOG_DIR/*
    ;;

  *)
  echo "Usage: gitolite_ctl {start|stop|logs|tail|clearlogs}" ;;
esac
exit 0
